# HomeClaw Boot Checklist

On gateway restart, verify HomeClaw is operational:

1. Check if HomeClaw app is running (`homeclaw-cli status`)
2. If not running, log a warning — webhooks will fail until the app starts
3. Read `memory/MEMORY.md` and yesterday's daily log to restore context
